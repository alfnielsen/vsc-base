import * as assert from 'assert'

import * as vsc from '../../vsc-base-development/vsc-base'

// suite('webview_initWebview', () => {
//    test(' 1', () => {
//       const r1 = vsc.initWebview()
//       assert.equal(r1, '')
//    })
// })

// suite('webview_setupWebviewConnection', () => {
//    test(' 1', () => {
//       const r1 = vsc.setupWebviewConnection()
//       assert.equal(r1, '')
//    })
// })

// suite('webview_startWebview', () => {
//    test(' 1', () => {
//       const r1 = vsc.startWebview()
//       assert.equal(r1, '')
//    })
// })

// suite('webview_WebviewHTMLTemplate', () => {
//    test(' 1', () => {
//       const r1 = vsc.WebviewHTMLTemplate()
//       assert.equal(r1, '')
//    })
// })
